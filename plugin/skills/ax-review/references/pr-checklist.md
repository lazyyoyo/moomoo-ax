# pr 리뷰 체크리스트 (v0.1 stub)

PR 리뷰 체크리스트. 후속 스프린트(`ax-deploy` 도입 시)에서 작성.

본 v0.1에서는 `ax-review pr` 호출 시 스킬이 다음 한 줄을 출력하고 즉시 중단한다:

```
NOT_IMPLEMENTED: ax-review pr 타입은 v0.1 미구현. 후속 스프린트(ax-deploy 도입 시) 작성 예정.
```
