# v0.3 Phase A.7 end-to-end 실행 기록

- 날짜: 2026-04-13
- run_id: `1e9ae5a4f9b3`
- ndjson: `.harness/runs/1e9ae5a4f9b3.ndjson`
- run_dir (--keep 보존): `/var/folders/r8/.../T/ax-run-1e9ae5a4f9b3`
- fixture: `labs/ax-implement/input/static-nextjs-min` (plain 디렉토리, copytree 격리)
- 드라이버: `scripts/ax_product_run.py --fixture ... --keep`

## 한 줄 결론

> Phase A.7 **PASS**. 격리 static fixture 에서 `ax-implement` 가 3 태스크를 subagent 로 위임·구현·리뷰·커밋하고 `run_review_checks.sh` blocking gate 를 실제로 통과. 수동 확인 완료.

## 3회 시도 이력

| # | run_id | 결과 | 원인 |
|---|---|---|---|
| 1 | `8ffd95a30897` | 실패 | `parse_stream_json` 의 content block 이 string 일 때 `.get()` 예외 |
| 2 | `f16376c8b190` | 3.6s / $0 / turns 2 | SKILL.md 의 `` !`복합명령` `` preflight 가 `Bash(which:*)` prefix 매칭 거부 |
| 3 | `2d263949fb2c` | $5.68 / turns 62 / 17분 | `run_review_checks.sh` 의 allowed-tools 경로가 `${CLAUDE_SKILL_DIR}` 치환 안 돼 permission 거부 → LLM 이 reviewer 자연어 검증으로 우회 |
| **4** | **`1e9ae5a4f9b3`** | **$3.32 / turns 63 / 11분 / PASS** | 본 run |

## 수정 사항 (3 → 4 run 사이)

1. `scripts/ax_product_run.py` — `build_allowed_tools(plugin_dir)` 함수로 `plugin_dir.resolve()` 기반 Bash 절대경로 패턴 생성. env 변수 치환 안 되는 permission rule 제약 해소.
2. `tests/test_ax_product_run.py` — mock 기반 8개 테스트 신규 (allowed_tools 구성 / cwd 전달 / stdout_path 기록 / cleanup / --keep / fixture 원본 보존 / 거부 케이스).
3. fixture `package.json` — `eslint` 9.15.0 → 8.57.1. flat config 대신 legacy `.eslintrc.json` 사용 (eslint-config-next 15 와 호환).
4. gitleaks 8.30.1 설치 (`brew install`).

## 검증 결과

### 1) `run_review_checks.sh` 실 tool_event

```
tool_use_id: toolu_01KiMwaHMRe1yzDdNwCoTk2M
command: bash /Users/sunha/hq/projects/moomoo-ax/plugin/skills/ax-implement/scripts/run_review_checks.sh 2>&1 | tail -30
```

✅ 1회 호출, tool event 에 정확히 기록됨.

### 2) run_review_checks exit code

**exit 0** (overall status: `pass`). 세부:

- `gitleaks`: exit 0, "no leaks found, 4 commits scanned, 297KB in 82ms"
- `eslint`: exit 0, empty output (warning 0개)
- `arch_compliance`: exit 0, declared-in-arch = installed-in-pkg 일치

### 3) run_checks (lint / typecheck / test / build / gitleaks)

빌드 iteration 중 `run_checks.sh` 여러 번 호출:

- `lint`: pass (eslint 8 + next/core-web-vitals, no warnings)
- `typecheck`: pass (`tsc --noEmit`, strict)
- `test`: pass (vitest 2.1.5, 4 케이스 — variant × 2 + click + disabled)
- `build`: pass (next 15.0.3 production build)
- `gitleaks`: pass (review gate 내부)

### 4) plan.md / git log / 생성 코드 육안 확인

**plan.md**: preflight 3개 + T-001 / T-002 / T-003 전부 `[x]`. fix task 없음 (첫 시도에 전부 APPROVE).

**git log (worktree 안)**:

```
23ecb2c chore: mark T-001/T-002/T-003 complete in plan.md
0442b28 T-003: demo Button on home page
938c3c5 T-002: add Button unit tests
2b59924 T-001: add Button component with primary/secondary variants
f44f60c seed
```

seed + 3 task commit + 1 chore. 태스크당 커밋 규칙 준수.

**생성 코드 (`src/components/Button.tsx`)**:

```tsx
import type { CSSProperties, ReactNode } from "react";

export type ButtonVariant = "primary" | "secondary";
export interface ButtonProps { ... }

const baseStyle: CSSProperties = {
  paddingTop: "var(--space-2)",
  borderRadius: "var(--radius-md)",
  // ... 전부 토큰 var(--*) 경유
};

const primaryStyle: CSSProperties = { ...baseStyle, backgroundColor: "var(--color-primary)", ... };
```

- ✅ 토큰-only (하드코딩 없음)
- ✅ i18n copy 경유 (page.tsx 라벨은 `copy.home.*`)
- ✅ 두 variant 지원 (primary / secondary)
- ⚠️ reviewer 의 non-blocking 주의: `borderWidth: var(--radius-sm)` — token 카테고리 mismatch (border 용 token 없음). T-004 로 추후 조정 가능.

### 5) subagent 호출

| subagent | 호출 수 | 용도 |
|---|---|---|
| `team-ax:design-engineer` | 3 | T-001 / T-002 / T-003 구현 |
| `team-ax:reviewer` | 4 | 태스크별 리뷰 3 + cross-cutting 1 |

모두 **fully-qualified 네임스페이스** 로 호출됨 (Codex 4차 리뷰 R10 해소).

## 기타 관측

- **cost**: $3.32 (이전 시도 $5.68 대비 42% 감소 — preflight 수정으로 불필요 우회 로직 제거됨)
- **duration**: 11분 4초 (npm install 13s + 구현/리뷰/게이트)
- **tool 분포**: Bash 많음 (git / npm / script / server), Agent 7 (subagent), Edit / Read / Write 정상
- **dev 서버 검증**: localhost:3457 HTTP 200, 두 버튼 렌더 (primary "시작하기" / secondary "더 알아보기"), copy 경유 확인
- **moomoo-ax 원본 / fixture 템플릿 원본**: 미수정 (drift 없음)

## 남은 후속

- **A.7 plan.md 형식 잔재**: Edit tool 이 `- [ ] 체크` 형식 대신 헤더 뒤에 `[x]` 로 붙인 곳 존재 (`### T-001 ... [x]`). 본래는 `- [ ] T-001 ...` / `- [x] T-001 ...` 원칙. SKILL.md 프로토콜 명시 방식과 편차. v0.4 에서 `find_next_task` 같은 structured helper 재도입 검토 (vs LLM 재명시).
- reviewer 의 non-blocking note (`--radius-sm` 을 `borderWidth` 로 재사용) — 다음 dogfooding 때 태스크로 등장 가능성.
- `run_review_checks.sh` 가 `| tail -30` 로 파이프된 건 LLM 의 출력 절약 의도. gate 실효성엔 영향 없음 (exit code 만 판정에 사용).

## 결론

v0.3 Phase A 의 end-to-end 검증 **PASS**. SKILL.md / scripts / subagents / 드라이버 / fixture 전부 실제로 맞물려 동작함을 실증. Codex 4차 리뷰 3개 findings (R10/R11/R12) 모두 실전 해소 확인.

다음: Phase B 마감 (report / HANDOFF / BACKLOG / PR / v0.3.0 태그).
